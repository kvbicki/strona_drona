import React from "react";

const Header = () => (
  <header className="header">
    <h1>DronyPro</h1>
    <nav>
      <a href="#hero">Home</a>
      <a href="#drones">Drony</a>
      <a href="#about">O nas</a>
    </nav>
  </header>
);

export default Header;
